name: vehicle_key_verifier
description: UK Vehicle Key Research & Verification Tool
publish_to: "none"

version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  http: ^1.2.1
  shared_preferences: ^2.2.3
  sqflite: ^2.3.3
  path: ^1.9.0
  archive: ^3.6.1
  file_picker: ^10.3.10
  path_provider: ^2.1.3
  url_launcher: ^6.3.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/
